#include <iostream>
#include <algorithm> // Include this header for std::max
using namespace std;

int main() {
    int a = 10;
    int b = 20;
    int maximum = max(a, b); // std::max returns the greater of a and b

    cout << "The maximum value is: " << maximum << endl;
    return 0;
}
